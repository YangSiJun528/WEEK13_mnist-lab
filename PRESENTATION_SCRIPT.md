# MNIST 전략 비교 실험 발표 대본

## 3분 발표 대본

이번 실험에서는 넘파이(NumPy)만으로 구현한 엠니스트(MNIST) 분류 모델에서, 학습 과정에서 가장 높은 결과를 보인 기준 조건을 잡고 다른 선택지를 사용했을 때 성능이 어떻게 달라지는지 비교했습니다.

모델 구조는 입력 784개에서 은닉층 512개, 은닉층 256개를 거쳐 10개 클래스를 예측하는 완전연결 신경망입니다. 활성화 함수는 렐루(ReLU), 초기화 방식은 히 초기화(He initialization)로 고정했습니다. 기준 조건은 아담(Adam), 학습률 0.001, 배치 정규화(BatchNorm) 사용, 드롭아웃(Dropout) 사용 조합입니다. 여기서 옵티마이저(optimizer), 배치 정규화, 드롭아웃, 학습률(learning rate)을 하나씩 바꿔 총 6가지 실험을 진행했습니다.

먼저 전체 결과입니다. 가장 높은 검증 정확도(validation accuracy)는 배치 정규화를 제거한 `no_batchnorm`에서 나온 98.52퍼센트였습니다. 하지만 기준 조건인 `adam_baseline`도 98.50퍼센트였기 때문에 차이는 0.02퍼센트 포인트입니다. 이 정도 차이는 단일 실행 편차 범위에 가까워서, 최고 정확도 하나만으로 결론을 내리기보다는 검증 손실(validation loss)과 학습-검증 차이(train-validation gap)를 함께 봤습니다.

첫 번째 비교는 에스지디(SGD)와 아담(Adam)입니다. 여기서는 차이가 가장 크게 나타났습니다. 아담 기준 모델은 최종 검증 정확도 98.50퍼센트를 기록했고, 에스지디는 94.42퍼센트에 머물렀습니다. 에스지디도 학습은 진행됐지만 20 에포크(epoch) 안에서는 목표 정확도 97퍼센트에 도달하지 못했습니다. 같은 조건에서는 아담이 훨씬 빠르게 수렴했습니다.

두 번째 비교는 배치 정규화와 드롭아웃 유무입니다. 배치 정규화를 제거해도 정확도 자체는 크게 떨어지지 않았습니다. 다만 검증 손실은 기준 모델보다 높았습니다. 이 결과에서는 배치 정규화의 차이가 정확도보다 손실 안정성 쪽에서 더 잘 드러났습니다.

드롭아웃을 제거한 경우에는 흐름이 더 분명했습니다. 학습 정확도(train accuracy)는 빠르게 올라갔지만, 검증 손실이 가장 높았습니다. 학습-검증 차이(train-validation gap)도 초반부터 크게 벌어졌습니다. 다만 드롭아웃을 켠 모델에서도 gap은 어느 정도 커집니다. 중요한 것은 gap이 커졌다는 사실 자체가 아니라, gap이 커지는 속도와 검증 성능이 얼마나 오래 개선되는지입니다. 드롭아웃을 끈 경우에는 gap이 빠르게 커지고 검증 성능이 일찍 정체됐습니다. 반대로 드롭아웃을 켠 경우에는 gap이 더 천천히 커지고 검증 성능 개선이 더 오래 이어졌습니다.

세 번째 비교는 학습률입니다. 아담에서 학습률 0.001을 고정한 기준 모델은 최종 검증 정확도 98.50퍼센트로 가장 높았습니다. 학습률 감소(learning rate decay)를 적용한 모델은 98.37퍼센트로 조금 낮았습니다. 하지만 차이는 0.13퍼센트 포인트로 작고, 학습-검증 차이는 학습률 감소 모델이 더 작았습니다. 그래서 정확도만 보면 기준 모델이 근소하게 앞서지만, 일반화 안정성까지 보면 학습률 감소 전략이 더 안정적입니다.

마지막으로 정리하겠습니다. 기준 조건인 아담 학습률 0.001, 배치 정규화 사용, 드롭아웃 사용 조합은 final validation accuracy 기준으로 가장 높은 결과를 냈습니다. 다른 선택지와 비교해보면, 아담은 에스지디보다 빠르게 수렴했고, 배치 정규화는 정확도보다 손실 안정성에서 차이가 보였으며, 드롭아웃은 gap이 커지는 속도를 늦추고 검증 성능 개선을 더 오래 유지하는 데 도움이 됐습니다. 학습률 감소 전략은 최종 정확도는 조금 낮았지만 gap이 더 작아서 일반화 안정성 측면에서 의미 있는 대안이었습니다. 전체적으로 실험 결과는 학습한 내용과 대체로 맞는 방향으로 나왔습니다.
