# MNIST 전략 비교 실험 발표 대본

## 3분 발표 대본

먼저 프로젝트 요약부터 말씀드리겠습니다. 이번 실험은 넘파이(NumPy)만으로 구현한 엠니스트(MNIST) 분류 모델에서, 학습 과정에서 가장 높은 결과를 보인 기준 조건을 잡고 다른 선택지를 사용했을 때 성능이 어떻게 달라지는지 비교한 실험입니다.

기준 조건은 `adam_baseline`입니다. 아담(Adam), 학습률 0.001, 배치 정규화(BatchNorm) 사용, 드롭아웃(Dropout) 사용 조합입니다. 이 조건의 최종 검증 정확도(validation accuracy)는 98.50퍼센트였습니다. 최고 검증 정확도는 배치 정규화를 제거한 조건(`no_batchnorm`)에서 98.52퍼센트로 나왔지만, 차이가 0.02퍼센트 포인트라서 이 숫자만으로 더 좋다고 보기는 어렵습니다. 그래서 검증 손실(validation loss)과 학습-검증 차이(train-validation gap)도 같이 봤습니다.

다음은 모델 구조입니다. 입력은 28 곱하기 28 이미지를 펼친 784차원 벡터입니다. 은닉층은 512개, 256개 두 개를 사용했고, 각 은닉층에는 어파인(Affine), 배치 정규화, 렐루(ReLU), 드롭아웃을 순서대로 적용했습니다. 출력층은 10개 클래스를 예측하는 소프트맥스(Softmax) 구조입니다. 여기서 렐루와 히 초기화(He initialization)는 모든 실험에서 고정했습니다.

학습 설정은 20 에포크(epoch), 배치 크기(batch size) 128, 드롭아웃 비율 0.5로 맞췄습니다. 총 6개의 조건을 비교했고, 기준 조건에서 한 가지 선택지만 바꾸는 방식으로 실험했습니다. 그래서 결과 차이가 옵티마이저(optimizer), 배치 정규화, 드롭아웃, 학습률(learning rate) 중 어디에서 오는지 나눠서 볼 수 있게 했습니다.

전체 결과 표를 보면, 아담을 사용한 조건들은 대부분 98퍼센트 이상의 검증 정확도를 보였습니다. 반면 에스지디(SGD)는 94.42퍼센트에 머물렀습니다. 가장 큰 차이는 옵티마이저 비교에서 나왔고, 아담이 20 에포크 안에서 훨씬 빠르게 수렴했습니다.

이제 비교 실험 부분입니다. 첫 번째는 에스지디와 아담 비교입니다. 같은 구조에서 옵티마이저만 바꿨을 때, 아담 기준 모델은 98.50퍼센트였고 에스지디는 94.42퍼센트였습니다. 에스지디도 학습은 진행됐지만 목표 정확도 97퍼센트에는 도달하지 못했습니다. 이 결과는 아담이 더 빠르게 수렴한다는 내용과 맞게 나왔습니다.

두 번째는 배치 정규화와 드롭아웃 유무 비교입니다. 배치 정규화를 제거해도 정확도 손실은 크지 않았습니다. 하지만 검증 손실은 기준 모델보다 높았습니다. 그래서 배치 정규화의 차이는 정확도보다 손실 안정성 쪽에서 더 잘 보였습니다.

드롭아웃 비교는 gap 자체보다 gap이 커지는 속도와 검증 성능의 흐름을 같이 봐야 합니다. 드롭아웃을 끈 조건에서는 gap이 빠르게 커지고, 검증 성능이 비교적 일찍 정체됐습니다. 반대로 드롭아웃을 켠 조건에서는 gap이 더 천천히 커지고, 검증 성능 개선이 더 오래 이어졌습니다. 즉 드롭아웃을 켰는데도 gap이 커졌다는 사실만으로 실패라고 볼 수는 없습니다. gap이 얼마나 빨리 커지는지, 그리고 검증 정확도 최고점이 좋아졌는지를 같이 봐야 합니다.

세 번째는 학습률 비교입니다. 학습률 0.001을 고정한 기준 조건은 최종 검증 정확도 98.50퍼센트로 가장 높았습니다. 학습률 감소(learning rate decay)를 적용한 조건은 98.37퍼센트로 조금 낮았지만, 학습-검증 차이는 더 작았습니다. 그래서 정확도만 보면 기준 조건이 앞서고, 일반화 안정성까지 보면 학습률 감소 조건도 의미 있는 대안입니다.

마지막 결론입니다. 기준 조건인 아담, 학습률 0.001, 배치 정규화 사용, 드롭아웃 사용 조합은 최종 검증 정확도 기준으로 가장 높은 결과를 냈고, 다른 조건을 비교하기 위한 기준으로 적절했습니다. 비교 결과도 학습한 내용과 대체로 맞았습니다. 아담은 에스지디보다 빠르게 수렴했고, 드롭아웃 제거는 과적합 신호를 강하게 만들었고, 학습률 설정은 최종 정확도와 일반화 차이에 모두 영향을 주었습니다.
