# MNIST 전략 비교 실험 발표 대본

## 3분 발표 대본

안녕하세요. 이번 발표에서는 넘파이(NumPy)만으로 구현한 엠니스트(MNIST) 분류 모델에서 어떤 학습 전략이 성능에 영향을 주는지 비교한 결과를 설명하겠습니다.

모델의 기본 구조는 입력 784개에서 은닉층 512개, 다시 은닉층 256개를 거쳐 최종 10개 클래스를 예측하는 구조입니다. 활성화 함수는 렐루(ReLU), 초기화 방식은 히 초기화(He initialization)로 고정했습니다. 대신 옵티마이저(optimizer), 배치 정규화(BatchNorm), 드롭아웃(Dropout), 학습률(learning rate)을 바꿔가며 총 6가지 실험을 진행했습니다.

먼저 가장 높은 성공률을 보면, 배치 정규화를 제거한 실험(no_batchnorm)에서 최고 검증 정확도(best validation accuracy)가 98.52퍼센트로 가장 높게 나왔습니다. 하지만 기준 모델(adam_baseline)의 최종 검증 정확도(final validation accuracy)도 98.50퍼센트였습니다. 두 값의 차이는 0.02퍼센트 포인트밖에 되지 않기 때문에, 이 결과만으로 배치 정규화를 제거하는 편이 더 좋다고 보기는 어렵습니다.

그래서 이번 실험에서는 성공률 하나만 보지 않고, 검증 손실(validation loss)과 학습-검증 차이(train-validation gap)도 같이 비교했습니다. 단일 실행에서는 초기값이나 미니배치 순서 때문에 0.1에서 0.2퍼센트 포인트 정도 차이는 생길 수 있기 때문입니다.

첫 번째 비교는 에스지디(SGD)와 아담(Adam)입니다. 이 비교에서는 차이가 가장 뚜렷했습니다. 아담 기준 모델은 최종 검증 정확도가 98.50퍼센트였고, 에스지디는 94.42퍼센트에 머물렀습니다. 에스지디도 학습은 진행됐지만, 20 에포크(epoch) 안에서는 충분히 수렴하지 못했습니다. 따라서 이번 모델에서는 아담이 훨씬 빠르게 수렴했다고 볼 수 있습니다.

두 번째 비교는 배치 정규화와 드롭아웃 유무입니다. 배치 정규화를 제거해도 정확도 자체는 크게 떨어지지 않았습니다. 다만 검증 손실은 기준 모델보다 높았습니다. 즉 배치 정규화의 효과는 정확도보다 손실 안정성에서 더 잘 보였습니다.

드롭아웃을 제거한 경우는 조금 다릅니다. 학습 정확도(train accuracy)는 매우 빠르게 올라갔지만, 검증 손실은 가장 높았습니다. 학습-검증 차이도 초반부터 크게 벌어졌습니다. 여기서 중요한 점은 차이가 커진다는 것만으로 무조건 나쁘다고 보는 것이 아니라, 검증 정확도와 검증 손실을 같이 봐야 한다는 점입니다. 기준 모델은 차이가 조금 늘어나도 검증 정확도가 계속 좋아졌지만, 드롭아웃 제거 모델은 학습 데이터에는 빠르게 맞으면서 검증 기준의 일반화 성능은 상대적으로 나빠졌습니다. 그래서 이 경우가 과적합(overfitting) 경향이 가장 뚜렷했습니다.

세 번째 비교는 학습률입니다. 아담에서 학습률 0.001을 고정한 기준 모델은 최종 검증 정확도 98.50퍼센트로 가장 높았습니다. 반면 학습률 감소(learning rate decay)를 적용한 모델은 98.37퍼센트로 조금 낮았습니다. 하지만 차이는 0.13퍼센트 포인트로 작고, 학습-검증 차이는 학습률 감소 모델이 더 작았습니다. 따라서 정확도만 보면 기준 모델이 근소하게 앞서지만, 일반화 안정성까지 보면 학습률 감소 전략이 더 안정적인 후보라고 볼 수 있습니다.

정리하면, 옵티마이저는 아담이 에스지디보다 확실히 좋았습니다. 배치 정규화는 정확도 차이보다 손실 안정성에서 차이가 보였습니다. 드롭아웃은 과적합을 줄이는 데 중요한 역할을 했습니다. 최종 정확도만 기준으로 하면 아담 학습률 0.001 기준 모델이 가장 높았지만, 반복 실행 편차와 일반화 차이까지 고려하면 아담 학습률 감소 전략을 최종 후보로 잡는 것이 더 안정적인 선택이라고 판단했습니다.
